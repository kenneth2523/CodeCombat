# Stay in the middle and defend!

loop:
    enemy = self.findNearestEnemy()
    if enemy:
        self.attack(enemy)
        self.attack(enemy)
        # Either attack the enemy...
        
        
    else:
        self.moveXY(40, 34)# ... or move back to your defensive position.
        
        
